name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'  # This triggers the workflow for version tags like v1.0.0

permissions:
  contents: write
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.23.2'

      - name: Build binary
        run: |
          mkdir -p build
          go build -o ./bin/beacon ./cmd/beacon/beacon.go
          go build -o ./bin/install ./cmd/beacon/install.go

      - name: Create release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload beacon binary
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./bin/beacon
          asset_name: beacon-${{ github.sha }}-linux-amd64
          asset_content_type: application/octet-stream

      - name: Upload install binary
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./bin/install
          asset_name: install-${{ github.sha }}-linux-amd64
          asset_content_type: application/octet-stream